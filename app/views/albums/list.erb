<div class="form-group" >
  <%= @albums %>
</div>
<div class="form-group">
  <table>
    <th>View and album</th>
    <tr>
      <% @albums.each do |album| %>
      <td class="<%= album.id %>"><a href="/albums/<%= album.id %>"><%= album.name %></a>
      </td>
    </tr>
        <% end %>

</table>
</div>